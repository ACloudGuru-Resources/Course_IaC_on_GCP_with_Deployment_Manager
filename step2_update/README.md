# Update deployment

## Overview

The file virtucon-updates.yaml is a Google Cloud Deployment Manager deployment example that updates the virtucon-environment deployment with modified startup-script in vm-template.py file from previous step 1.
